# Markdown → Mermaid.ai 整体流程：C4 L1 / L2 / L3

本文描述同一个安装包中的 CLI、HTTP、MCP 三入口链路：

> 人或 AI 可以通过 CLI/MCP 管理 Mermaid 链接；Typora、VS Code 或 Obsidian 通过普通 HTTP 链接打开图。本机服务读取对应 Mermaid 代码块的最新源码，再通过已开启 9222 的 Chrome/CDP 把源码写入 Mermaid.ai 共用草稿。

## 阅读顺序

| 层级 | 回答的问题 | 本文视角 |
|---|---|---|
| L1 — System Context | 谁在使用系统？系统依赖谁？ | 使用者、AI Host、Markdown 编辑器、本机桥接系统、Chrome、Mermaid.ai |
| L2 — Containers | 系统由哪些可运行单元和存储组成？ | CLI Adapter、MCP Adapter、HTTP Bridge Service、本地文件与状态 |
| L3 — Components | HTTP Bridge Service 如何处理编辑器点击和 MCP 打开？ | 认证、签名解析、串行注入、目标页匹配、验证、重试与失败呈现 |

---

## L1 — System Context

系统边界只看「Markdown → Mermaid.ai Link System」整体，不展开内部 Python 模块。

```mermaid
flowchart LR
    person_user["使用者<br/>阅读 Markdown、点击图表链接"]
    external_agent["AI Host<br/>Codex / Claude / Cursor"]
    external_editor["Markdown 编辑器<br/>Typora / VS Code / Obsidian"]
    external_files["本地 Markdown 文档<br/>Mermaid 源码 + 稳定签名链接"]
    system_bridge["Markdown → Mermaid.ai Link System<br/>CLI + HTTP + MCP 共用核心逻辑"]
    external_chrome["日常或专用 Chrome + CDP<br/>接收系统外链并暴露目标标签通道"]
    external_mermaid["Mermaid.ai<br/>Code Editor + 共用 __inject_scratch__"]

    person_user -->|"阅读与点击"| external_editor
    person_user -->|"运行 CLI"| system_bridge
    person_user -->|"向 AI 下达任务"| external_agent
    external_agent -->|"MCP stdio Tools"| system_bridge
    external_editor -->|"读取文档"| external_files
    external_editor -->|"把本机 HTTP 链接交给系统浏览器"| external_chrome
    external_chrome -->|"GET 127.0.0.1:38473/v1/open"| system_bridge
    system_bridge -->|"按 path + block_id 读取最新代码块"| external_files
    system_bridge -->|"通过 CDP 驱动本次点击页签"| external_chrome
    external_chrome -->|"HTTPS 打开并编辑共用草稿"| external_mermaid

    classDef person fill:#DBEAFE,stroke:#2563EB,color:#111827,stroke-width:2px
    classDef system fill:#EDE9FE,stroke:#7C3AED,color:#111827,stroke-width:2px
    classDef external fill:#F3F4F6,stroke:#6B7280,color:#111827,stroke-width:2px
    classDef store fill:#DCFCE7,stroke:#16A34A,color:#111827,stroke-width:2px

    class person_user person
    class system_bridge system
    class external_agent,external_editor,external_chrome,external_mermaid external
    class external_files store
```

### L1 关键结论

- Markdown 编辑器只负责打开普通 HTTP 链接，不需要专用插件。
- CLI、HTTP、MCP 是三个 Adapter，背后共用同一个 Application Interface 和浏览器 Bridge。
- MCP 进程由 AI Host 临时启动；常驻 HTTP Bridge 独立存在，因此关闭 AI 不影响 Markdown 链接。
- 本机桥接系统是唯一的自动化边界：它读取源码并控制 Chrome，但不直接调用 Mermaid.ai 私有 API。
- Mermaid.ai 只保留一张共用草稿；「哪条链接对应哪张图」由本机签名链接中的文件路径和稳定 `block_id` 决定。

---

## L2 — Containers

这一层展开可独立运行的进程与持久化文件。注入器不是独立服务，而是运行在 HTTP Bridge Service 进程内。

```mermaid
flowchart TB
    person_user["使用者"]
    external_agent["AI Host<br/>Codex / Claude / Cursor"]
    external_editor["Markdown 编辑器<br/>Typora / VS Code / Obsidian"]
    external_chrome["日常或专用 Chrome 进程<br/>CDP :9222"]
    external_mermaid["Mermaid.ai SaaS<br/>共用 __inject_scratch__"]

    subgraph bridge_system ["Markdown → Mermaid.ai Link System"]
        container_cli["CLI Adapter<br/>按需 Python 进程<br/>sync / list / doctor / open"]
        container_mcp["MCP Adapter<br/>AI Host 启动的 stdio 进程<br/>结构化 Tools"]
        container_service["HTTP Bridge Service<br/>常驻 Python 进程<br/>127.0.0.1:38473"]
        store_docs["Markdown Files<br/>源码 + 签名链接"]
        store_secret["Config + HMAC Secret<br/>edit_url / CDP / 签名密钥"]
        store_runtime["Runtime State<br/>PID + 持久日志"]
    end

    person_user -->|"运行 sync / start / status"| container_cli
    person_user -->|"向 AI 下达任务"| external_agent
    external_agent -->|"stdio MCP"| container_mcp
    person_user -->|"阅读与点击"| external_editor

    container_cli -->|"读取并原位更新"| store_docs
    container_cli -->|"创建或读取签名密钥"| store_secret
    container_mcp -->|"list / sync"| store_docs
    container_mcp -->|"读取配置并派生控制认证"| store_secret
    container_mcp -->|"认证控制请求：open"| container_service

    external_editor -->|"读取"| store_docs
    external_editor -->|"系统外链"| external_chrome
    external_chrome -->|"GET 签名链接"| container_service
    container_service -->|"等待页 + 一次性 marker URL"| external_chrome

    container_service -->|"点击时读取最新 Mermaid 块"| store_docs
    container_service -->|"读取 edit_url / CDP / secret"| store_secret
    container_service -->|"写任务结果、重试与错误"| store_runtime
    container_service -->|"单目标 CDP WebSocket"| external_chrome
    external_chrome -->|"HTTPS + Monaco 编辑"| external_mermaid

    classDef person fill:#DBEAFE,stroke:#2563EB,color:#111827,stroke-width:2px
    classDef container fill:#EDE9FE,stroke:#7C3AED,color:#111827,stroke-width:2px
    classDef external fill:#F3F4F6,stroke:#6B7280,color:#111827,stroke-width:2px
    classDef store fill:#DCFCE7,stroke:#16A34A,color:#111827,stroke-width:2px

    class person_user person
    class container_cli,container_mcp,container_service container
    class external_agent,external_editor,external_chrome,external_mermaid external
    class store_docs,store_secret,store_runtime store
```

### L2 容器职责

| 容器 / 存储 | 职责 | 不负责 |
|---|---|---|
| CLI Adapter | 为人和脚本提供 `sync/list/doctor/open/start/stop` | 不实现 Markdown 或浏览器业务逻辑 |
| MCP Adapter | 把相同能力以结构化 Tools 暴露给 AI Host | 不监听新端口、不独占 Chrome |
| HTTP Bridge Service | 校验点击、创建任务、串行注入、重试、呈现最终结果页 | 不保存 Mermaid 源码快照 |
| Markdown Files | 保存权威 Mermaid 源码 | 不保存运行任务状态 |
| Config + HMAC Secret | 保存固定草稿 URL、CDP 地址和本机签名密钥 | 不进入 Markdown 链接明文 |
| Runtime State | 保存 PID 与长期诊断日志 | 不决定链接指向哪个代码块 |

---

## L3 — Components

这一层只放大 `HTTP Bridge Service`。一次点击的成功标准是：预览已出现当前代码块的证据、加载遮罩已移除、地址栏一次性 marker 已清理。

```mermaid
flowchart TB
    external_chrome["已开启 CDP 的 Chrome<br/>本次点击页签"]
    external_mcp["MCP Adapter<br/>open_diagram"]
    store_docs["Markdown File"]
    store_secret["HMAC Secret + Config"]
    store_log["Persistent Log"]

    subgraph service_boundary ["HTTP Bridge Service"]
        component_http["HTTP Adapter<br/>/v1/open + /v1/jobs + /v1/control/open"]
        component_resolver["Signed Link + Block Resolver<br/>校验 HMAC、path、block_id"]
        component_jobs["Injection Job Module<br/>串行、重试、任务取代、最终结果"]
        component_browser["Chrome / Mermaid.ai Adapter<br/>预检 + opaque target + 一次注入尝试"]
        component_success["Typed Receipt<br/>edit URL + preview evidence"]
        component_superseded["Typed Supersession<br/>非失败、不可重试"]
        component_failure["Automation Error<br/>由 Job Module 决定是否重试"]
        component_logger["Job Logger<br/>记录 job_id、attempt、error"]
    end

    external_chrome -->|"GET 签名链接"| component_http
    external_mcp -->|"POST 控制请求 + 派生认证"| component_http
    component_http -->|"校验并定位"| component_resolver
    component_resolver -->|"读取签名密钥"| store_secret
    component_resolver -->|"按稳定 ID 读取当前代码块"| store_docs
    component_http -->|"创建并启动任务"| component_jobs
    component_http -->|"MCP 打开：直接进入串行注入锁"| component_jobs
    component_http -->|"返回等待页与 marker URL"| external_chrome

    component_jobs -->|"每次尝试重新解析最新源码"| component_resolver
    component_jobs -->|"prepare opaque target"| component_browser
    component_resolver -->|"最新 Mermaid 源码"| component_browser
    component_browser -->|"单目标 CDP + Monaco + 预览验证"| external_chrome
    store_secret -->|"完整浏览器配置仅由 Adapter 持有"| component_browser

    component_browser -->|"验证成功"| component_success
    component_success -->|"state = succeeded"| component_jobs

    component_browser -->|"观察任务取代"| component_superseded
    component_superseded -->|"state = superseded"| component_jobs
    component_browser -.->|"CDP / Monaco / 预览故障"| component_failure
    component_failure -->|"retry policy"| component_jobs
    component_jobs -->|"最终 failed / superseded 时请求精确导航"| component_browser
    component_browser -->|"导航本机结果页"| external_chrome

    component_jobs --> component_logger
    component_failure --> component_logger
    component_logger --> store_log

    classDef component fill:#EDE9FE,stroke:#7C3AED,color:#111827,stroke-width:2px
    classDef external fill:#F3F4F6,stroke:#6B7280,color:#111827,stroke-width:2px
    classDef store fill:#DCFCE7,stroke:#16A34A,color:#111827,stroke-width:2px
    classDef failure fill:#FEE2E2,stroke:#DC2626,color:#111827,stroke-width:2px
    classDef success fill:#D1FAE5,stroke:#059669,color:#111827,stroke-width:2px

    class component_http,component_resolver,component_jobs,component_browser,component_logger component
    class component_failure failure
    class component_success,component_superseded success
    class external_chrome,external_mcp external
    class store_docs,store_secret,store_log store
```

### 一次点击的关键时序

1. 编辑器点击通过签名 `GET /v1/open` 进入；MCP 打开通过派生认证 `POST /v1/control/open` 进入。
2. 两种入口都重新校验签名和当前代码块位置，并共用唯一串行注入锁。
3. Chrome / Mermaid.ai Adapter 完成 CDP 预检查并返回 opaque target；Bridge 只保存 capability 与导航 URL，不理解 marker 格式。
4. Adapter 只连接目标页自身的 CDP WebSocket，不 attach 或初始化同一 Chrome 的其他页面；MCP 打开则在后台查找或创建固定草稿页。
5. Adapter 写入 Monaco 并验证预览；编辑器点击成功时再移除遮罩和 marker，并返回 typed receipt。
6. Bridge 决定是否重试；新任务取代旧任务时，Adapter 返回 typed supersession，旧任务进入独立的 `superseded` 状态而不是伪装成失败。
7. MCP 调用以结构化成功结果或 Tool Error 返回，不让 MCP 进程独占 Chrome。

## 设计不变量

- 每个 Mermaid 代码块恰好一条 App 链接。
- 链接只保存 `path + block_id` 的签名引用，不保存源码快照。
- 点击时读取磁盘上的最新源码。
- 多次点击由单一注入锁串行化，避免共用草稿并发写入。
- 任务取代是独立结果，不通过错误字符串或 `failed` 状态表达。
- Bridge 不读取浏览器配置、一次性 marker 或目标标签 locator；这些知识只存在于 Chrome / Mermaid.ai Adapter。
- CLI 与 MCP 的 `open` 必须通过认证控制入口复用同一个 Bridge。
- 控制认证由 HMAC Secret 派生，不在请求中发送真实 Secret。
- 旧图只能作为底层共用草稿存在；加载遮罩消失前不能被当作本次结果。
- 成功后地址栏必须精确恢复固定 Mermaid.ai edit URL。
- 最终失败必须可见、可重试、可从日志诊断。
